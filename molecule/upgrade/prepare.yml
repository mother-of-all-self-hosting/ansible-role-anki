# SPDX-FileCopyrightText: 2026 Slavi Pantaleev
#
# SPDX-License-Identifier: AGPL-3.0-or-later

---
- name: Prepare Anki synchronization server upgrade Molecule tests
  hosts: all
  become: true
  gather_facts: true
  tasks:
    - name: Ensure apt cache is updated for Anki synchronization server upgrade Molecule tests preparation
      ansible.builtin.apt:
        update_cache: true
        cache_valid_time: 600
      when: ansible_facts["os_family"] == 'Debian'

    - name: Ensure required packages are installed for Anki synchronization server upgrade Molecule tests preparation
      ansible.builtin.package:
        name:
          # Required by community.docker's modules, which the role uses to pull
          # the container image.
          - python3-requests
          # The sync protocol compresses request and response bodies with zstd,
          # which the sync probe needs in order to talk to the server.
          - python3-zstandard
        state: present

    - name: Ensure the Anki synchronization server sync probe is installed for upgrade Molecule tests preparation
      ansible.builtin.copy:
        src: "{{ lookup('ansible.builtin.env', 'MOLECULE_PROJECT_DIRECTORY') }}/molecule/sync-probe.py"
        dest: /usr/local/bin/anki-sync-probe
        mode: "0755"

    - name: Ensure Docker is installed for Anki synchronization server upgrade Molecule tests preparation
      ansible.builtin.include_role:
        name: ansible-role-docker
      vars:
        docker_daemon_options:
          storage-driver: vfs
